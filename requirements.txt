# numpy is pinned explicitly (it's otherwise only a transitive dependency) because
# several packages below have a hard floor below which they're NumPy-2-incompatible
# at the binary/ABI level, not just the API level -- a loose resolver can otherwise
# pick a pre-NumPy-2 wheel for one of them while installing NumPy 2.x for another,
# producing "numpy.core.multiarray failed to import" / "np.unicode_ was removed in
# the NumPy 2.0 release" at runtime instead of at install time. Verified floors:
# pandas 2.2.2, pyarrow 16.0, scikit-learn 1.4.2, scipy 1.13.0.
numpy>=2.0,<3
pandas>=2.2.2
requests>=2.31
python-dotenv>=1.0
jupyter>=1.0
tenacity>=8.2
tqdm>=4.66
pyarrow>=16.0
pyyaml>=6.0
openpyxl>=3.1
scikit-learn>=1.4.2
scipy>=1.13.0
jenkspy>=0.4
geopandas>=1.0
shapely>=2.0
libpysal>=4.12
esda>=2.6
matplotlib>=3.8
streamlit>=1.50
plotly>=6.0
pytest>=8.0
pytest-mock>=3.14
